diff --git a/GAME_RULES.txt b/GAME_RULES.txt
new file mode 100644
index 0000000..5392761
--- /dev/null
+++ b/GAME_RULES.txt
@@ -0,0 +1,436 @@
+LOS 7 MARES - REGLES (MODE AVANCE) - REFERENCE POUR L'IMPLEMENTATION GODOT
+============================================================================
+Ce fichier resume les regles du jeu de plateau "Los 7 Mares" telles que
+fournies par l'auteur, pour servir de reference lors du developpement/debug
+du projet Godot. Il ne decrit PAS l'etat actuel du code, seulement les
+regles a implementer. Les points marques [A FAIRE / A CLARIFIER] sont des
+zones encore floues ou incompletes dans les notes d'origine.
+
+------------------------------------------------------------
+1. APERCU GENERAL
+------------------------------------------------------------
+- Partie = 7 manches maximum (mode avance).
+- Chaque manche = 2 cycles de tours de joueur, dans le sens horaire.
+- A son tour, un joueur choisit 2 actions en placant son capitaine ou son
+  officier sur le plateau action (1 pion par tour, donc 2 tours = 2 pions).
+- Fin de manche -> Phase de maintenance -> manche suivante.
+- Fin de partie quand il n'y a plus de fortune disponible sur le plateau
+  action a la fin d'une manche.
+- Victoire : le joueur avec le plus de points de gloire.
+
+------------------------------------------------------------
+2. LES 7 MERS
+------------------------------------------------------------
+Chaque mer a une repartition de types de cartes (Exploration / Combat /
+Commerce) et une ou deux ressources principales.
+
+- Mer de Glace     : Exploration 40% / Combat 40% / Commerce 20%  -> Nourriture
+- Mer Maudite       : Exploration 20% / Combat 60% / Commerce 20%  -> Bois
+- Mer Sauvage       : Exploration 60% / Combat 20% / Commerce 20%  -> Nourriture & Bois
+- Mer d'Azuro       : Exploration 33% / Combat 33% / Commerce 33%  -> Rhum
+- Mer de Feu        : Exploration 20% / Combat 40% / Commerce 40%  -> Toile & Metal
+- Mer de Jade       : Exploration 40% / Combat 20% / Commerce 40%  -> Bois & Metal
+- Mer d'Abondance   : Exploration 20% / Combat 20% / Commerce 60%  -> Nourriture & Toile
+
+Chaque paquet mer contient 36 cartes en mode avance.
+[A FAIRE] Chaque mer doit afficher sa tuile correspondante pour chaque
+joueur (visuel a placer).
+
+------------------------------------------------------------
+3. PLATEAU JOUEUR
+------------------------------------------------------------
+- 7 emplacements pour gemmes (rose des gemmes, un par mer).
+- Cache au tresor (zone de stockage des tresors).
+- Reserve de fortune (zone de stockage des fortunes).
+- Niveau de voile : 1 par defaut + 4 ameliorations possibles (niveaux 2-5).
+- Niveau d'armes : 1 par defaut + 4 ameliorations possibles (niveaux 2-5).
+- Coque du bateau : 7 planches par defaut.
+- Cale du bateau : 9 cases de stockage de ressources (nourriture, bois,
+  toile, acier/metal, rhum). Tout excedent au-dela de 9 ressources doit
+  etre defausse (voir "Bateau surcharge").
+- 3 pistes de cartes a droite du plateau :
+  - Bleue   = Exploration
+  - Rouge   = Combat
+  - Brune   = Commerce (actuellement appelee "claire" dans le code/assets,
+    [A FAIRE] renommer en "Commerce"/"brune" partout)
+  Une carte multi-couleur peut etre rangee dans n'importe laquelle de ses
+  pistes correspondantes, au choix du joueur. Le decalage visuel des cartes
+  dans une piste est purement cosmetique (permet aux autres joueurs de
+  voir le nombre de cartes), aucun effet de regle.
+
+------------------------------------------------------------
+4. PLATEAU ACTION (plateau central)
+------------------------------------------------------------
+- 7 emplacements dores contenant chacun 1 fortune au depart : indiquent le
+  nombre de manches restantes. Le dernier joueur de chaque manche en
+  prend une -> la 8e manche ne peut jamais commencer.
+- 4 actions principales : Naviguer en mer, Debarquer sur une ile,
+  Acceder a un port, Renover son bateau.
+- 5 emplacements d'action ou les pions (capitaine/officier) se placent,
+  chacun lie a 2 actions principales.
+- Espaces bonus pour les jetons bonus (1/2/3 jetons selon 2/3-4/5 joueurs).
+
+Force d'un emplacement (determine si l'action est principale ou reduite) :
+- Emplacement FORT (action principale) : emplacement vide, ou on place son
+  capitaine avec un/des officier(s) adverses.
+- Emplacement FAIBLE (action reduite) : on place son capitaine avec un/des
+  capitaine(s) (adverses), ou on place son officier avec n'importe quel(s)
+  autre(s) pion(s).
+- Un joueur ne peut pas placer ses 2 pions sur le meme emplacement, mais
+  peut partager un emplacement deja occupe par d'autres joueurs.
+- Aucun ordre impose entre capitaine/officier sur les 2 tours du joueur.
+
+------------------------------------------------------------
+5. MISE EN PLACE (SETUP)
+------------------------------------------------------------
+1. Placer le plateau action, 1 fortune par emplacement dore.
+2. Melanger et placer aleatoirement les 7 tuiles mer autour du plateau.
+   Placer 1/2/3 jetons bonus selon 2/3-4/5 joueurs.
+3. Melanger chaque paquet de 36 cartes, le placer pres de sa tuile (laisser
+   un espace pour les cartes revelees).
+4. Reveler la 1ere carte de chaque paquet. Si carte tresor revelee, la
+   remplacer jusqu'a n'avoir que des cartes sans tresor.
+5. Chaque joueur choisit une couleur, prend : plateau joueur, 1 cachette,
+   1 bateau, 2 pions action, 7 gemmes, 1 perroquet (cote "libre"). Placer
+   ses 7 gemmes sur les tuiles mer correspondantes (icones en haut de
+   tuile = emplacements gemmes).
+6. Mettre de cote : ressources, fortunes restantes, tresors, jetons
+   amelioration, des, marqueur doré (1er joueur).
+7. Chaque joueur lance simultanement 1 de combat + 1 de exploration.
+   Meilleur jet -> marqueur doré (1er joueur). Egalite -> on relance entre
+   joueurs a egalite. Ordre de comparaison : des noirs priment
+   (canon > abordage > rien), puis des blancs departagent
+   (2 etoiles > 1 etoile > rien). Le dernier joueur recoit 1 fortune.
+8. Dans le sens ANTI-horaire, du dernier au 1er joueur : chaque joueur
+   place sa cachette sur un espace inoccupe entre 2 mers, puis place son
+   bateau sur sa cachette.
+-> La 1ere manche commence.
+
+------------------------------------------------------------
+6. TOUR DU JOUEUR (deroulement detaille)
+------------------------------------------------------------
+1. Rafistoler son bateau (ssi chavire) : redresser le bateau, +3 planches.
+2. Choisir ses actions principales : placer 1 pion (capitaine ou officier,
+   celui non encore joue) sur un emplacement du plateau action.
+3. Effectuer ses 2 actions, l'une apres l'autre, dans l'ordre choisi. Une
+   fois une action commencee, impossible de revenir a la precedente.
+   Hierarchie : principale > reduite (si applicable) > par defaut. Le
+   joueur peut toujours choisir volontairement de descendre d'un cran.
+   En plus, 1 action OPTIONNELLE possible par tour (Commercer avec un
+   joueur ou Attaquer un joueur), non remplacable par une action par
+   defaut. Maximum 3 actions/tour (2 principales/reduites/defaut + 1
+   optionnelle).
+4. Finir son tour :
+   a. Si le bateau est sur sa cachette : +1 planche, +1 nourriture,
+      +1 bois.
+   b. Si aucune carte recuperee ni bateau ameliore ce tour : +1 fortune
+      (compensation).
+5. Si le joueur a gauche a encore un pion a jouer -> son tour commence.
+   Sinon -> Fin de la manche.
+
+------------------------------------------------------------
+7. FIN DE MANCHE / PHASE DE MAINTENANCE
+------------------------------------------------------------
+S'il reste >= 1 fortune sur le plateau action -> Phase de maintenance :
+1. Defausser toutes les cartes rencontre restantes (vers defausse commune
+   ou sous leur paquet).
+2. Reveler une nouvelle carte dans chaque mer qui n'en a pas.
+3. Recuperer tous les pions action du plateau action.
+4. Le 1er joueur prend 1 fortune du plateau action, passe le marqueur
+   doré au joueur a sa gauche.
+5. Nouvelle manche.
+
+S'il n'y a plus de fortune -> Fin de la partie (voir section 8).
+
+------------------------------------------------------------
+8. FIN DE PARTIE ET SCORE
+------------------------------------------------------------
+Points de gloire :
+1. Gemmes       : 2 par gemme, +2 bonus si les 7 gemmes sont collectees.
+2. Tresors      : 2 par tresor.
+3. Fortunes     : 1 par lot de 2 fortunes.
+4. Voile        : 1/3/5/7 pts pour niveau 2/3/4/5.
+5. Armes        : 1/3/5/7 pts pour niveau 2/3/4/5.
+6. Planches     : 3 pts si 7 planches, 1 pt si 5 ou 6 planches.
+7. Ressources   : 1 pt par lot de 3 ressources (toutes confondues).
+8. Jetons bonus : 1 pt chacun, utilise ou non.
+9. Perroquets   : 2 pts pour son propre perroquet + 1 pt par perroquet
+   d'un autre joueur possede.
+10. Pistes de cartes (Exploration / Combat / Commerce), classement separe
+    par piste selon le nombre de cartes :
+    - 1ere place = autant de points que de joueurs dans la partie
+    - 2e place   = (nombre de joueurs - 2), minimum 0
+    - 3e place   = (nombre de joueurs - 4), minimum 0
+    Le rang d'un joueur = nombre de joueurs STRICTEMENT mieux classes que
+    lui (pas sa position dans l'ordre de jeu). En cas d'egalite a une
+    place, TOUS les joueurs egaux recoivent les points de cette place, et
+    le(s) suivant(s) recoivent les points de la place directement
+    inferieure (ex: si N joueurs sont 1ers ex-aequo, le(s) suivant(s)
+    recoit/recoivent les points de la 2e place, jamais de la 3e).
+
+Egalite au score final -> Combat final :
+- Chaque joueur ajuste son bateau a 7 planches et 5 canons (armes niv 5).
+- Interdiction d'utiliser rhum, fortune, ou tout autre effet.
+- Gagnant = celui qui gagne a la Phase des canons OU la Phase d'abordage.
+- Si aucun vainqueur, on repete le combat (coque conservee) jusqu'a
+  obtenir un vainqueur.
+
+------------------------------------------------------------
+9. ACTIONS PRINCIPALES
+------------------------------------------------------------
+Regles generales :
+- Tout composant recu/gagne/collecte vient de la reserve generale.
+- Tout composant defausse/donne/depense/paye/perdu retourne a la reserve.
+- Les des sont TOUJOURS lances ensemble, par type de de, max 5 des par
+  type.
+- Un symbole "succes" sur une carte = obtenir un canon (boulet) OU un
+  abordage sur un de combat.
+
+DEBARQUER SUR UNE ILE (necessite d'etre dans une mer avec une carte ile) :
+- Ile sauvage (planche bleue, Exploration) : lancer des d'exploration
+  (1er de gratuit, +1 de par nourriture depensee) pour >= etoiles requises.
+- Ile amicale (Exploration OU Commerce, planche brune) : choix entre
+  Exploration (comme ci-dessus) ou suivre les etapes "Commerce".
+- Ile hostile (Exploration OU Combat, planche rouge) : choix entre
+  Exploration, ou lancer autant de des combat que le niveau d'armes pour
+  >= succes requis.
+-> Reussite ou echec (voir section 10).
+
+ACCEDER A UN PORT (necessite mer avec carte port + ressources suffisantes) :
+- Port ordinaire (Commerce, planche brune) : payer les ressources
+  demandees (a gauche de la fleche). 1 rhum peut remplacer n'importe
+  quelle ressource demandee, ou 2 ressources d'un autre type peuvent en
+  remplacer 1 (substitution possible meme si on a deja la ressource
+  demandee). -> Reussir une activite.
+- Port perilleux (Matelotage + Commerce, bleu+brun) : lancer autant de des
+  exploration que le niveau de voile pour >= etoiles requises (+1 de si
+  1 nourriture depensee, max +1). Si etoiles insuffisantes : perdre
+  autant de planches que d'etoiles manquantes.
+- Port malfame (Combat + Commerce, rouge+brun) : lancer autant de des
+  combat que le niveau d'armes pour >= succes requis. Reussite -> suivre
+  Commerce normalement. Echec -> suivre Commerce mais recevoir moins de
+  ressources = nombre de succes manquants.
+
+RENOVER SON BATEAU (necessite ressources suffisantes OU bateau
+endommage) : choisir Ameliorer OU Reparer (pas les deux, sauf en
+depensant 1 rhum pour faire les deux) :
+- Ameliorer : choisir piste voile ou armes. Payer le cout de
+  l'emplacement vide le plus a gauche de la piste choisie, placer un
+  jeton amelioration. Voiles = bois + toile ; Armes = bois + acier. Une
+  seule amelioration a la fois.
+  Cout (niveau vise -> bois / toile ou acier) :
+    Niveau 2 -> 1 bois / 1
+    Niveau 3 -> 1 bois / 2
+    Niveau 4 -> 1 bois / 3
+    Niveau 5 (max) -> 1 bois / 4
+- Reparer : +1 planche gratuite, puis +1 planche par bois depense
+  (ex: 2 bois depenses -> 3 planches au total). Toile et/ou metal peuvent
+  remplacer le bois pour cette reparation.
+
+NAVIGUER EN MER :
+- Points de mouvement de depart = niveau de voile. Chaque point sert a
+  Deplacer son bateau OU Explorer la mer actuelle.
+- Deplacer son bateau (1 pt) : vers case adjacente (tuile mer ou sa
+  cachette). Pas de limite de bateaux par tuile mer ; cachette accessible
+  uniquement a son proprietaire.
+  - Vers mer sans carte revelee -> DOIT reveler une carte.
+  - Vers mer avec ile/port -> PEUT reveler une carte.
+  - Vers mer avec rencontre -> DOIT resoudre la rencontre.
+- Explorer la mer actuelle (1 pt, necessite mer sans rencontre) : reveler
+  une carte mer sur la case du bateau.
+- Cas special : rencontre deja presente en debut d'action -> la resoudre
+  immediatement avant tout autre chose.
+- Fin de l'action Naviguer quand : rencontre reussie (carte + recompenses
+  recues), plus de mouvement, plus de planches (bateau chavire), ou choix
+  du joueur d'arreter.
+- Reveler une carte mer : piocher la 1ere carte du paquet correspondant,
+  la placer sur la pile revelee. Rencontre revelee -> DOIT etre resolue.
+  Impossible de reveler si une rencontre est deja presente (la resoudre
+  d'abord).
+- Resoudre la rencontre : determiner Amicale ou Dangereuse, puis choisir
+  Gerer ou Eviter (rencontre dangereuse inevitable -> DOIT etre geree).
+  - Rencontre amicale : Eviter = defausser la carte, revenir a l'action.
+  - Rencontre dangereuse : Eviter = defausser 1 fortune + la carte,
+    revenir a l'action.
+  - Dans les 2 cas : possibilite de Gerer la rencontre (voir ci-dessous).
+
+GERER UNE RENCONTRE :
+- Bateau pirate (dangereuse, planche rouge) :
+  1. Phase jet de des : jet du pirate indique sur la carte (boulets +
+     abordages). Faces "inconnues" (case noire, point d'interrogation) ->
+     un autre joueur (en principe celui a gauche) lance autant de des
+     combat que de faces inconnues, ajoutees au jet du pirate. Puis le
+     joueur lance ses propres des combat = son niveau d'armes.
+  2. Phase des canons : les 2 "bateaux" perdent des planches = nb de
+     boulets adverses (voir "Perdre X planches"). Pour le pirate, comparer
+     directement nb de boulets du joueur vs nb de planches du pirate (>=
+     -> pirate a 0 planche). Si pirate a 0 planche et joueur en a encore
+     -> victoire -> Reussir une activite. Si le joueur chavire -> Echouer
+     une activite. Sinon -> Phase d'abordage.
+  3. Phase abordage : si joueur a strictement plus d'abordages que le
+     pirate -> victoire -> Reussir une activite. Sinon -> defaite : le
+     pirate vole 1 ressource par abordage en exces qu'il a par rapport au
+     joueur (voir "Perdre X ressources"), puis -> Echouer une activite.
+- Menace meteo (dangereuse, bleu+gris) : lancer des exploration = niveau
+  de voile pour >= etoiles requises (+1 de si 1 nourriture depensee, max
+  +1) -> Reussir/Echouer.
+- Geant des mers (dangereuse, rouge+gris) : lancer des combat = niveau
+  d'armes pour >= faces requises -> Reussir/Echouer.
+- Creature des mers (dangereuse, bleu+rouge+gris) : lancer des exploration
+  (niveau de voile, +1 nourriture -> +1 de max) ET des combat (niveau
+  d'armes) simultanement pour >= etoiles ET >= faces requises ->
+  Reussir/Echouer.
+- Bateau marchand (amicale, brun+rouge) : choix entre Commerce (suivre
+  etapes Commerce) ou Attaquer (lancer des combat = niveau d'armes pour
+  >= succes requis) -> Reussir/Echouer.
+- Flotte marchande (amicale, brun) : uniquement Commerce (les attaquer
+  serait perdant) -> suivre etapes Commerce.
+
+------------------------------------------------------------
+10. MECANIQUES TRANSVERSALES
+------------------------------------------------------------
+REUSSIR UNE ACTIVITE :
+1. Recuperer la carte dans la piste correspondant a l'activite effectuee
+   (si carte a 2 activites/couleurs possibles, choisir la piste liee a
+   l'activite reellement faite).
+2. Recevoir les recompenses indiquees (choix si plusieurs ressources
+   possibles). Si total > 9 ressources -> Bateau surcharge (voir plus
+   bas). Puis recompense bonus selon le nombre de cartes collectees dans
+   CETTE mer :
+   - 1ere carte de la mer -> recuperer la gemme de cette mer.
+   - 2e carte de la mer -> recuperer le jeton bonus de cette mer (s'il en
+     reste), le placer face "effet" au-dessus du plateau joueur.
+   - 3e carte de la mer ou plus -> rafraichir son jeton bonus de cette mer
+     (le retourner face "effet") si on le possede deja.
+3. Fin de l'action -> passer a l'action suivante ou finir son tour.
+
+ECHOUER UNE ACTIVITE :
+1. Recevoir 1 fortune en compensation.
+2. Si la carte a un effet negatif (planche grise) -> le subir. Exemples
+   d'effets negatifs : perdre X ressources, perdre X planches, perdre 1
+   planche par ressource possedee, perdre 1 planche par niveau d'armes,
+   ne plus pouvoir agir ce tour, ne plus pouvoir faire certaines actions
+   indiquees ce tour, perdre 1 planche + 1 amelioration de voile, perdre
+   4 nourriture puis autant de planches que de nourriture manquante.
+   [A FAIRE] Detailler/lister precisement les icones d'effet negatif pour
+   chaque carte (icones a formaliser dans les donnees de carte / catalog).
+3. Defausser la carte si c'est une rencontre ; sinon la laisser en place.
+4. Verifier l'etat : si bateau chavire -> fin de tour immediate. Sinon si
+   mouvement restant -> reprendre la navigation. Sinon -> fin de l'action.
+
+BATEAU SURCHARGE : si > 9 ressources sur le bateau, choisir 9 ressources a
+garder et jeter le surplus.
+
+PERDRE X PLANCHES : retirer X planches de la coque (hors plateau, zone
+"perdues temporairement"). Si 0 planche restante -> Bateau chavire.
+
+BATEAU CHAVIRE : positionner le bateau sur le cote (etat "chavire").
+Personne ne peut interagir avec un joueur chavire. Perdre la moitie des
+ressources (arrondi a l'inferieur) -> "Perdre X ressources".
+
+PERDRE X RESSOURCES : prendre toutes ses ressources en main, les etaler ;
+defausser X ressources en commencant par celles les plus eloignees du
+joueur ; recuperer le reste. Si un adversaire fait chavirer un bateau en
+combat entre joueurs, les ressources perdues lui sont donnees au lieu
+d'etre defaussees.
+
+RHUM (motivation d'equipage) : pendant un jet de des, depenser 1 rhum
+max par jet pour +1 de additionnel (limite : 1 rhum par jet ; ne
+s'applique pas au commerce).
+
+FORTUNE (fixer des des) : avant de lancer, depenser des fortunes pour
+fixer des faces au lieu de lancer : 1 fortune = 1 face etoile ou 1 face
+canon ; 2 fortunes = 1 face double-etoile ou 1 face abordage. On fixe des
+des deja possedes (pas d'achat de de supplementaire). Impossible de fixer
+apres le lancer. Les des non fixes restants sont lances normalement.
+
+JETONS BONUS : effet unique reutilisable apres l'avoir "regagne" (en
+recuperant une nouvelle carte de la mer correspondante). Une fois
+utilise, retourner le jeton face cachee.
+  - Mer de Glace     : +1 point de mouvement lors de Naviguer en mer.
+  - Mer Sauvage      : avant le jet d'exploration, fixer un de a 2 etoiles.
+  - Mer d'Azuro      : remplace la depense de 1 rhum.
+  - Mer Maudite      : remplace la depense de 1 fortune.
+  - Mer de Feu       : +1 de combat additionnel avant le jet de combat.
+  - Mer d'Abondance  : apres un Commerce via carte, +1 ressource du meme
+    type qu'une des ressources deja recues.
+  - Mer de Jade      : lors de recuperation de planches, +2 planches
+    gratuites supplementaires.
+  Note : un jeton bonus utilisable ne permet PAS d'obtenir l'element
+  correspondant directement sur le bateau (c'est un effet, pas une
+  ressource stockable).
+
+------------------------------------------------------------
+11. ACTIONS PAR DEFAUT (si principale/reduite impossible ou refusee)
+------------------------------------------------------------
+- Pecher en mer   : +1 nourriture.
+- Attendre sa fortune : +1 fortune.
+
+------------------------------------------------------------
+12. ACTIONS REDUITES (emplacement faible du plateau action)
+------------------------------------------------------------
+- Caboter en mer : Naviguer en mer mais avec seulement 1 point de
+  mouvement, quel que soit le niveau de voile.
+- Rabibocher son bateau (necessite bateau endommage) : +1 planche
+  gratuite uniquement, pas d'amelioration ni de planche supplementaire.
+- Collecter sur une ile (necessite mer avec ile) : recevoir 1 ressource
+  au choix parmi celles indiquees (nourriture/bois) sur l'activite
+  exploration de la carte. Pas de tresor ; la carte reste en place.
+- Travailler au port (necessite mer avec port) : recevoir 1 ressource au
+  choix parmi celles a droite de la fleche d'echange. Pas de tresor ; la
+  carte reste en place.
+
+------------------------------------------------------------
+13. ACTIONS OPTIONNELLES (1 max par tour, avant/apres une action
+    principale/reduite, non remplacable par une action par defaut)
+------------------------------------------------------------
+COMMERCER AVEC UN JOUEUR : choisir un joueur dans la meme mer.
+- S'il accepte : echange libre de ressources/fortunes/tresors/perroquets,
+  sans limite de quantite. Action terminee.
+- S'il refuse : action annulee, on peut retenter avec un autre joueur de
+  la meme mer ou faire une autre action optionnelle.
+
+ATTAQUER UN JOUEUR : choisir un adversaire dans la meme mer.
+- Tentative d'evasion (si l'adversaire veut fuir) : il lance des
+  exploration = son niveau de voile (pas de de additionnel possible). Si
+  strictement plus d'etoiles que le niveau de voile de l'attaquant ->
+  evasion reussie, action terminee. Sinon -> combat.
+- Combat entre joueurs (similaire au combat pirate) :
+  1. Phase jet de des : chaque joueur lance des combat = son niveau
+     d'armes.
+  2. Phase des canons : chacun perd des planches = boulets adverses.
+     - Si un seul bateau chavire : le perdant recoit 1 fortune ; le
+       gagnant vole 1 perroquet + les ressources defaussees par le
+       perdant.
+     - Si les deux chavirent : chacun recoit 1 fortune.
+     - Sinon -> phase d'abordage.
+  3. Phase d'abordage :
+     - Si un joueur a strictement plus d'abordages : le perdant recoit
+       1 fortune ; le gagnant vole 1 perroquet, puis vole
+       ressources/tresors selon l'exces d'abordages (1 face = 1
+       ressource volee, 2 faces = 1 tresor vole ; chaque face en exces
+       n'est utilisee qu'une fois, le gagnant choisit la repartition).
+     - Sinon (egalite) : chacun recoit 1 fortune.
+  Perroquets voles a d'autres joueurs : toujours places cote "en cage".
+  Le sien propre reste cote "libre".
+  Si le bateau de l'attaquant/attaque chavire pendant ce combat -> fin de
+  tour immediate pour lui.
+
+------------------------------------------------------------
+14. POINTS OUVERTS / A CLARIFIER (releves dans les notes d'origine)
+------------------------------------------------------------
+- Renommer la piste "claire" en "Commerce" / couleur brune partout dans
+  le projet (UI, assets, code).
+- Afficher la tuile mer correspondante pour chaque joueur (visuel a
+  faire) lors du setup.
+- Detailler precisement, carte par carte, les icones d'effet negatif
+  (planche grise) dans le catalogue de cartes (card_catalog / game_card).
+- Verifier que la logique de "force d'emplacement" (fort/faible) au
+  placement des pions est bien implementee selon les regles exactes de
+  la section 4.
+- Verifier l'ordre exact des etapes de fin de tour (planche/cachette,
+  compensation fortune) par rapport a l'implementation actuelle du
+  game_flow / piece_placement_phase / action_resolution_phase.
+
+============================================================================
+Fin du document. A mettre a jour si les regles evoluent.
diff --git a/scenes/board/board.tscn b/scenes/board/board.tscn
index 155f599..75d1a2a 100644
--- a/scenes/board/board.tscn
+++ b/scenes/board/board.tscn
@@ -16,7 +16,7 @@
 [ext_resource type="PackedScene" uid="uid://bg437po33iydc" path="res://scenes/ui/player_setup_popup.tscn" id="14_dj4mb"]
 [ext_resource type="PackedScene" uid="uid://dt0ffi66gqwvd" path="res://scenes/board/action_spot.tscn" id="15_kug5n"]
 [ext_resource type="Script" uid="uid://b0a7tt2orhfwo" path="res://scripts/board/narration_box.gd" id="15_xu7li"]
-[ext_resource type="PackedScene" uid="uid://cawaiel7vey2f" path="res://scenes/ui/piece_selection_panel.tscn" id="16_bo4hw"]
+[ext_resource type="PackedScene" uid="uid://cawaiel7vey2f" path="res://scenes/ui/pion_selection_panel.tscn" id="16_bo4hw"]
 [ext_resource type="PackedScene" uid="uid://bn3g7mmfggktb" path="res://scenes/ui/sea_card_popup.tscn" id="17_bo4hw"]
 [ext_resource type="PackedScene" uid="uid://b058e8m5tg4kt" path="res://scenes/ui/player_board_expanded.tscn" id="18_ctilb"]
 [ext_resource type="Texture2D" uid="uid://dedw6tc0y6d80" path="res://assets/art/ui/back.svg" id="19_0avwi"]
@@ -25,7 +25,7 @@
 [ext_resource type="Script" uid="uid://cnxrqg83d7ymu" path="res://scripts/debug/debug_heptagon.gd" id="22_j2n54"]
 [ext_resource type="Script" uid="uid://da4v1q020p4x" path="res://scripts/board/dealing_phase.gd" id="23_s0ety"]
 [ext_resource type="Script" uid="uid://da2oyotyr3s1s" path="res://scripts/board/hideout_phase.gd" id="24_0avwi"]
-[ext_resource type="Script" uid="uid://dhx64n8v51ihg" path="res://scripts/board/piece_placement_phase.gd" id="25_jhx4t"]
+[ext_resource type="Script" uid="uid://dhx64n8v51ihg" path="res://scripts/board/pion_placement_phase.gd" id="25_jhx4t"]
 [ext_resource type="Script" uid="uid://dtqgyitf5s8n7" path="res://scripts/board/card_draw_phase.gd" id="26_m0x5n"]
 [ext_resource type="Script" uid="uid://crp8o7l2w4rxs" path="res://scripts/board/action_resolution_phase.gd" id="29_x4jil"]
 
@@ -155,7 +155,7 @@ theme_override_constants/separation = 8
 
 [node name="PlayerSetupPopup" parent="UI" unique_id=1537230509 instance=ExtResource("14_dj4mb")]
 
-[node name="PieceSelectionPanel" parent="UI" unique_id=1299012961 instance=ExtResource("16_bo4hw")]
+[node name="PionSelectionPanel" parent="UI" unique_id=1299012961 instance=ExtResource("16_bo4hw")]
 
 [node name="PlayerBoardsPile" type="Control" parent="UI" unique_id=1975439067]
 layout_mode = 3
@@ -326,7 +326,7 @@ script = ExtResource("23_s0ety")
 [node name="HideoutPhase" type="Node" parent="." unique_id=1991991205]
 script = ExtResource("24_0avwi")
 
-[node name="PiecePlacementPhase" type="Node" parent="." unique_id=1465754435]
+[node name="PionPlacementPhase" type="Node" parent="." unique_id=1465754435]
 script = ExtResource("25_jhx4t")
 
 [node name="CardDrawPhase" type="Node" parent="." unique_id=1152241436]
diff --git a/scenes/board/pieces/captain_piece.tscn b/scenes/board/pions/captain_pion.tscn
similarity index 72%
rename from scenes/board/pieces/captain_piece.tscn
rename to scenes/board/pions/captain_pion.tscn
index 7d7ff26..b7bdac9 100644
--- a/scenes/board/pieces/captain_piece.tscn
+++ b/scenes/board/pions/captain_pion.tscn
@@ -1,9 +1,9 @@
 [gd_scene format=3 uid="uid://b6oi3dmwjn7pf"]
 
 [ext_resource type="Texture2D" uid="uid://bgv5vlynaxpi" path="res://assets/art/pieces/capitaine.png" id="1_usv78"]
-[ext_resource type="Script" uid="uid://dsel5ru3snm3q" path="res://scripts/board/pieces/captain_piece.gd" id="2_rrd8q"]
+[ext_resource type="Script" uid="uid://dsel5ru3snm3q" path="res://scripts/board/pions/captain_pion.gd" id="2_rrd8q"]
 
-[node name="CaptainPiece" type="Sprite2D" unique_id=1290564754]
+[node name="CaptainPion" type="Sprite2D" unique_id=1290564754]
 texture = ExtResource("1_usv78")
 offset = Vector2(0, -150)
 script = ExtResource("2_rrd8q")
diff --git a/scenes/board/pieces/second_piece.tscn b/scenes/board/pions/officer_pion.tscn
similarity index 72%
rename from scenes/board/pieces/second_piece.tscn
rename to scenes/board/pions/officer_pion.tscn
index b1457bd..e5c0bce 100644
--- a/scenes/board/pieces/second_piece.tscn
+++ b/scenes/board/pions/officer_pion.tscn
@@ -1,9 +1,9 @@
 [gd_scene format=3 uid="uid://6pcl5yd2jb1q"]
 
 [ext_resource type="Texture2D" uid="uid://bqgogkq2x27de" path="res://assets/art/pieces/second.png" id="1_twy8u"]
-[ext_resource type="Script" uid="uid://bfspy5ogtssx0" path="res://scripts/board/pieces/second_piece.gd" id="2_1rmjk"]
+[ext_resource type="Script" uid="uid://bfspy5ogtssx0" path="res://scripts/board/pions/officer_pion.gd" id="2_1rmjk"]
 
-[node name="SecondPiece" type="Sprite2D" unique_id=1545191181]
+[node name="OfficerPion" type="Sprite2D" unique_id=1545191181]
 texture = ExtResource("1_twy8u")
 offset = Vector2(0, -133)
 script = ExtResource("2_1rmjk")
diff --git a/scenes/ui/piece_selection_panel.tscn b/scenes/ui/pion_selection_panel.tscn
similarity index 74%
rename from scenes/ui/piece_selection_panel.tscn
rename to scenes/ui/pion_selection_panel.tscn
index df07407..0bcd371 100644
--- a/scenes/ui/piece_selection_panel.tscn
+++ b/scenes/ui/pion_selection_panel.tscn
@@ -1,10 +1,10 @@
 [gd_scene format=3 uid="uid://cawaiel7vey2f"]
 
-[ext_resource type="Script" uid="uid://dc0gxdnkd1bpp" path="res://scripts/ui/piece_selection_panel.gd" id="1_j16g3"]
+[ext_resource type="Script" uid="uid://dc0gxdnkd1bpp" path="res://scripts/ui/pion_selection_panel.gd" id="1_j16g3"]
 [ext_resource type="Texture2D" uid="uid://bgv5vlynaxpi" path="res://assets/art/pieces/capitaine.png" id="1_xvjxm"]
 [ext_resource type="Texture2D" uid="uid://bqgogkq2x27de" path="res://assets/art/pieces/second.png" id="2_j16g3"]
 
-[node name="PieceSelectionPanel" type="Control" unique_id=1299012961]
+[node name="PionSelectionPanel" type="Control" unique_id=1299012961]
 layout_mode = 3
 anchors_preset = 0
 offset_right = 40.0
@@ -33,14 +33,14 @@ layout_mode = 2
 text = "Capitaine"
 horizontal_alignment = 1
 
-[node name="SecondOption" type="VBoxContainer" parent="IconsBox" unique_id=43043918]
+[node name="OfficerOption" type="VBoxContainer" parent="IconsBox" unique_id=43043918]
 layout_mode = 2
 
-[node name="SecondButton" type="TextureButton" parent="IconsBox/SecondOption" unique_id=1436843481]
+[node name="OfficerButton" type="TextureButton" parent="IconsBox/OfficerOption" unique_id=1436843481]
 layout_mode = 2
 texture_normal = ExtResource("2_j16g3")
 
-[node name="SecondLabel" type="Label" parent="IconsBox/SecondOption" unique_id=2068684674]
+[node name="OfficerLabel" type="Label" parent="IconsBox/OfficerOption" unique_id=2068684674]
 layout_mode = 2
-text = "Second"
+text = "Officier"
 horizontal_alignment = 1
diff --git a/scripts/autoload/game_flow.gd b/scripts/autoload/game_flow.gd
index 16e9b88..339f0e4 100644
--- a/scripts/autoload/game_flow.gd
+++ b/scripts/autoload/game_flow.gd
@@ -32,7 +32,7 @@ const MARKER_TEXTURE_PATH := "res://assets/art/pieces/marqueur.png"
 const BOAT_TEXTURE_PATH := "res://assets/art/pieces/bateau.png"
 const HULL_PLANKS_START := 7
 
-enum PieceRank { SECOND = 0, CAPTAIN = 1 }
+enum PionRank { OFFICER = 0, CAPTAIN = 1 }
 
 const TITLE_SCENE_PATH := "res://scenes/ui/title_screen.tscn"
 const BOARD_SCENE_PATH := "res://scenes/board/board.tscn"
@@ -146,21 +146,21 @@ func go_to_board() -> void:
 	get_tree().change_scene_to_file(BOARD_SCENE_PATH)
 
 
-func compute_case_color(pieces: Array) -> Color:
-	if pieces.is_empty():
+func compute_case_color(pions: Array) -> Color:
+	if pions.is_empty():
 		return Color(0, 0, 0, 0)
 	var max_rank: int = -1
-	for p in pieces:
+	for p in pions:
 		max_rank = max(max_rank, p["rank"])
-	var top_pieces := pieces.filter(func(p): return p["rank"] == max_rank)
-	top_pieces.sort_custom(func(a, b): return a["order"] < b["order"])
-	return COLOR_VALUES[top_pieces[0]["color"]]
+	var top_pions := pions.filter(func(p): return p["rank"] == max_rank)
+	top_pions.sort_custom(func(a, b): return a["order"] < b["order"])
+	return COLOR_VALUES[top_pions[0]["color"]]
 
 
 ## Range count éléments en cercle autour d'un centre (utilisé pour les
 ## pièces sur les action spots ET, avec un spacing différent, pour les
 ## bateaux regroupés sur une même mer, cf board.gd _relayout_boats).
-func layout_positions_for_case(count: int, spacing: float = UiTheme.CASE_PIECE_RADIUS, vertical_offset: Vector2 = Vector2(0, UiTheme.CASE_PIECE_VERTICAL_OFFSET)) -> Array[Vector2]:
+func layout_positions_for_case(count: int, spacing: float = UiTheme.CASE_PION_RADIUS, vertical_offset: Vector2 = Vector2(0, UiTheme.CASE_PION_VERTICAL_OFFSET)) -> Array[Vector2]:
 	match count:
 		0: return []
 		1: return [vertical_offset]
diff --git a/scripts/autoload/ui_theme.gd b/scripts/autoload/ui_theme.gd
index beba710..20197f7 100644
--- a/scripts/autoload/ui_theme.gd
+++ b/scripts/autoload/ui_theme.gd
@@ -11,9 +11,9 @@ const SELECTION_ICON_HEIGHT := 110.0
 const HOVER_TINT := Color(0.82, 0.82, 0.82)
 const CAMERA_SELECTION_SHIFT := 400.0
 const CAMERA_SELECTION_ZOOM := Vector2(0.22, 0.22)
-const CASE_PIECE_RADIUS := 100.0
-const CASE_PIECE_VERTICAL_OFFSET := 40.0
+const CASE_PION_RADIUS := 100.0
+const CASE_PION_VERTICAL_OFFSET := 40.0
 const CARD_POPUP_DURATION := 0.35
 const CARD_PILE_RADIUS_OFFSET := 900.0
 const DEPTH_DIRECTION := Vector2(-0.70710678, 0.70710678)
-const PIECE_SCALE := 0.6
+const PION_SCALE := 0.6
diff --git a/scripts/board/action_resolution_phase.gd b/scripts/board/action_resolution_phase.gd
index 428a4cc..7791631 100644
--- a/scripts/board/action_resolution_phase.gd
+++ b/scripts/board/action_resolution_phase.gd
@@ -1,7 +1,7 @@
 extends Node
 
 ## Déclenchée juste après qu'un joueur pose une pièce sur une case action
-## (piece_placement_phase.gd). Chaque case action donne accès à 2 des 4
+## (pion_placement_phase.gd). Chaque case action donne accès à 2 des 4
 ## actions du jeu ; le joueur choisit l'ordre, puis fait ou décline chaque
 ## action (déclin = 1 ressource nourriture OU 1 jeton fortune au choix).
 ## Seule l'action "déplacement" est pour l'instant implémentée ; les autres
@@ -99,7 +99,7 @@ func _run_decline() -> void:
 	else:
 		_player["special_resources"]["fortune"] += 1
 	GameFlow.players_changed.emit()
-	_board._autosave("pieces")
+	_board._autosave("pions")
 
 
 ## Déplace le bateau du joueur en dépensant jusqu'à sail_level points de
@@ -187,7 +187,7 @@ func _run_deplacement() -> void:
 	if _player.get("boat_sea", "") == "":
 		await _grant_hideout_reward()
 
-	_board._autosave("pieces")
+	_board._autosave("pions")
 
 
 ## Récompense de retour à la cachette : une planche de coque (plafonnée à
diff --git a/scripts/board/action_spot.gd b/scripts/board/action_spot.gd
index 5dc0a54..f055dde 100644
--- a/scripts/board/action_spot.gd
+++ b/scripts/board/action_spot.gd
@@ -6,8 +6,8 @@ const IDLE_SCALE_AMPLITUDE := 0.04  # la case grossit jusqu'à +4%, jamais en de
 const IDLE_SPEED := 1.2
 const HOVER_SCALE := 1.12
 const HOVER_DURATION := 0.15
-const PIECE_DROP_HEIGHT := 220.0
-const PIECE_DROP_DURATION := 0.35
+const PION_DROP_HEIGHT := 220.0
+const PION_DROP_DURATION := 0.35
 
 @onready var case_sprite: Sprite2D = $CaseSprite
 @onready var click_area: Area2D = $ClickArea
@@ -17,8 +17,8 @@ var _hover_tween: Tween
 var _hover_scale_factor: float = 1.0  # multiplicateur animé séparément par le tween de survol
 var _is_hovering: bool = false
 var hover_enabled: bool = false
-var _pieces: Array = []  # {color, rank, order, node}
-var _drop_tweens: Dictionary = {}  # piece_node -> Tween de chute en cours
+var _pions: Array = []  # {color, rank, order, node}
+var _drop_tweens: Dictionary = {}  # pion_node -> Tween de chute en cours
 var _drag_hover_color = null  # Color ou null : couleur du joueur qui drague, appliquée au survol par-dessus le zoom habituel
 
 
@@ -66,27 +66,27 @@ func _on_input_event(_viewport: Node, event: InputEvent, _shape_idx: int) -> voi
 		spot_clicked.emit(self)
 
 
-func _animate_piece_drop(piece_node: Node2D) -> void:
-	var target_position := piece_node.position
-	piece_node.position = target_position - Vector2(0, PIECE_DROP_HEIGHT)
-	piece_node.modulate.a = 0.0
-	var duration: float = Settings.anim_duration(PIECE_DROP_DURATION)
+func _animate_pion_drop(pion_node: Node2D) -> void:
+	var target_position := pion_node.position
+	pion_node.position = target_position - Vector2(0, PION_DROP_HEIGHT)
+	pion_node.modulate.a = 0.0
+	var duration: float = Settings.anim_duration(PION_DROP_DURATION)
 	var tween := create_tween()
 	tween.set_parallel(true)
-	tween.tween_property(piece_node, "position", target_position, duration)\
+	tween.tween_property(pion_node, "position", target_position, duration)\
 		.set_trans(Tween.TRANS_CUBIC).set_ease(Tween.EASE_OUT)
-	tween.tween_property(piece_node, "modulate:a", 1.0, duration * 0.7)
-	_drop_tweens[piece_node] = tween
+	tween.tween_property(pion_node, "modulate:a", 1.0, duration * 0.7)
+	_drop_tweens[pion_node] = tween
 	tween.finished.connect(func():
-		if _drop_tweens.get(piece_node) == tween:
-			_drop_tweens.erase(piece_node)
+		if _drop_tweens.get(pion_node) == tween:
+			_drop_tweens.erase(pion_node)
 	)
 
 
-func _relayout_pieces() -> void:
-	var positions: Array[Vector2] = GameFlow.layout_positions_for_case(_pieces.size())
-	for i in range(_pieces.size()):
-		var node: Node2D = _pieces[i]["node"]
+func _relayout_pions() -> void:
+	var positions: Array[Vector2] = GameFlow.layout_positions_for_case(_pions.size())
+	for i in range(_pions.size()):
+		var node: Node2D = _pions[i]["node"]
 		# Si cette pièce est encore en train de "tomber" (spam-clic rapide sur
 		# la case), on tue son tween avant de la replacer : sinon le tween,
 		# toujours actif, continue d'écrire une position obsolète par-dessus
@@ -101,14 +101,14 @@ func _relayout_pieces() -> void:
 	# en arrière, peu importe l'ordre d'ajout. On classe les indices par y croissant
 	# (arrière -> avant) et on attribue un z_index toujours >= 1, pour rester
 	# devant le sprite de la case (z_index 0).
-	var order := range(_pieces.size())
+	var order := range(_pions.size())
 	order.sort_custom(func(a, b): return positions[a].y < positions[b].y)
 	for rank in range(order.size()):
-		_pieces[order[rank]]["node"].z_index = rank + 1
+		_pions[order[rank]]["node"].z_index = rank + 1
 
 
 func _update_case_color() -> void:
-	var color: Color = GameFlow.compute_case_color(_pieces)
+	var color: Color = GameFlow.compute_case_color(_pions)
 	var base := color if color.a > 0.0 else Color.WHITE
 	if _is_hovering and _drag_hover_color != null:
 		case_sprite.modulate = _drag_hover_color
@@ -126,14 +126,14 @@ func set_drag_hover_color(color) -> void:
 
 
 ## Vrai si la souris est actuellement au-dessus de la collision de la case
-## (utilisé par piece_placement_phase.gd pour savoir où lâcher une pièce
+## (utilisé par pion_placement_phase.gd pour savoir où lâcher une pièce
 ## en drag & drop, en plus de l'effet de zoom au survol).
 func is_hovering() -> bool:
 	return _is_hovering
 
 
-func has_player_piece(color: String) -> bool:
-	for p in _pieces:
+func has_player_pion(color: String) -> bool:
+	for p in _pions:
 		if p["color"] == color:
 			return true
 	return false
@@ -147,26 +147,26 @@ func set_hover_enabled(enabled: bool) -> void:
 		_update_case_color()
 
 
-func add_piece(piece_node: Node2D, color: String, rank: int, animate: bool = true) -> void:
-	var order := _pieces.size()
-	_pieces.append({"color": color, "rank": rank, "order": order, "node": piece_node})
-	add_child(piece_node)
-	_relayout_pieces()
+func add_pion(pion_node: Node2D, color: String, rank: int, animate: bool = true) -> void:
+	var order := _pions.size()
+	_pions.append({"color": color, "rank": rank, "order": order, "node": pion_node})
+	add_child(pion_node)
+	_relayout_pions()
 	_update_case_color()
 	if animate:
-		_animate_piece_drop(piece_node)
+		_animate_pion_drop(pion_node)
 
 
-func get_pieces_snapshot() -> Array:
+func get_pions_snapshot() -> Array:
 	var out := []
-	for p in _pieces:
+	for p in _pions:
 		out.append({"color": p["color"], "rank": p["rank"]})
 	return out
 
 
 ## Retire toutes les pièces de la case (début d'un nouveau tour de jeu).
-func clear_pieces() -> void:
-	for p in _pieces:
+func clear_pions() -> void:
+	for p in _pions:
 		p["node"].queue_free()
-	_pieces.clear()
+	_pions.clear()
 	_update_case_color()
diff --git a/scripts/board/board.gd b/scripts/board/board.gd
index 5add6a9..49d09b6 100644
--- a/scripts/board/board.gd
+++ b/scripts/board/board.gd
@@ -6,8 +6,8 @@ const PILE_THUMB_OFFSET := Vector2(0, 6)
 const PLAYER_BOARDS_PANEL_MAX_HEIGHT_RATIO := 0.75
 
 const SEA_CARD_PILE_SCENE := preload("res://scenes/board/sea_card_pile.tscn")
-const CAPTAIN_PIECE_SCENE := preload("res://scenes/board/pieces/captain_piece.tscn")
-const SECOND_PIECE_SCENE := preload("res://scenes/board/pieces/second_piece.tscn")
+const CAPTAIN_PION_SCENE := preload("res://scenes/board/pions/captain_pion.tscn")
+const OFFICER_PION_SCENE := preload("res://scenes/board/pions/officer_pion.tscn")
 const SEA_TOKEN_PILE_SCENE := preload("res://scenes/board/sea_token_pile.tscn")
 const PLAYER_BOARD_ROW := preload("res://scenes/ui/player_board_row.tscn")
 
@@ -26,7 +26,7 @@ const SEA_KEY_BY_NODE_NAME := {
 @export var fortune_radius: float = 780.0
 @export var fortune_angle_start_degrees: float = -90.0
 @export var deck_stack_offset: Vector2 = Vector2(0, -2)
-@export var debug_skip_to_pieces: bool = false
+@export var debug_skip_to_pions: bool = false
 ## Échelle appliquée au sprite du jeton pour qu'il ait un rayon légèrement
 ## plus petit que celui de la tuile mer sur laquelle il repose. À ajuster
 ## dans l'inspecteur si besoin (sélectionner le noeud "Board").
@@ -46,7 +46,7 @@ const SEA_KEY_BY_NODE_NAME := {
 @onready var player_board_expanded: Control = $UI/PlayerBoardExpanded
 @onready var player_setup_popup: Control = $UI/PlayerSetupPopup
 @onready var narration_box: PanelContainer = $UI/NarrationBox
-@onready var piece_selection_panel: Control = $UI/PieceSelectionPanel
+@onready var pion_selection_panel: Control = $UI/PionSelectionPanel
 @onready var action_spots_container: Node2D = $ActionSpots
 @onready var hideout_spots_container: Node2D = $HideoutSpots
 @onready var fortune_spots_container: Node2D = $FortuneSpots
@@ -61,7 +61,7 @@ const SEA_KEY_BY_NODE_NAME := {
 
 @onready var dealing_phase: Node = $DealingPhase
 @onready var hideout_phase: Node = $HideoutPhase
-@onready var piece_placement_phase: Node = $PiecePlacementPhase
+@onready var pion_placement_phase: Node = $PionPlacementPhase
 @onready var card_draw_phase: Node = $CardDrawPhase
 @onready var return_to_menu_button: Button = $UI/ReturnToMenuButton
 @onready var return_to_menu_confirm: ConfirmationDialog = $UI/ReturnToMenuConfirm
@@ -204,25 +204,25 @@ func _ready() -> void:
 	hideout_phase.finished.connect(func():
 		_start_round()
 		_autosave("cards")
-		await piece_selection_panel.play_turn_announcement(GameFlow.round_number)
+		await pion_selection_panel.play_turn_announcement(GameFlow.round_number)
 		card_draw_phase.start(self)
 	)
 	card_draw_phase.finished.connect(func():
-		_autosave("pieces")
-		piece_placement_phase.start(self)
+		_autosave("pions")
+		pion_placement_phase.start(self)
 	)
-	piece_placement_phase.finished.connect(_on_round_finished)
+	pion_placement_phase.finished.connect(_on_round_finished)
 
 	if GameFlow.is_continuing:
 		_restore_from_save()
-	elif debug_skip_to_pieces:
+	elif debug_skip_to_pions:
 		narration_box.hide_box()
 		deck_area.visible = false
 		deck_area.input_pickable = false
 		for spot in hideout_spots_container.get_children():
 			spot.visible = false
 		_start_round()
-		piece_placement_phase.start(self)
+		pion_placement_phase.start(self)
 	elif GameFlow.pending_setup_mode != "":
 		deck_area.input_pickable = false
 		player_setup_popup.player_confirmed.connect(_on_setup_player_confirmed)
@@ -231,7 +231,7 @@ func _ready() -> void:
 		dealing_phase.start(self)
 
 
-## Bouton debug "Passer" (visible seulement si debug_skip_to_pieces) :
+## Bouton debug "Passer" (visible seulement si debug_skip_to_pions) :
 ## - si des boutons de choix sont affichés dans narration_box (résolution
 ##   d'action en cours), simule le choix le plus rapide (decline/stop) ;
 ## - sinon, une pose de pièce est en attente : la pose automatiquement sur
@@ -241,11 +241,11 @@ func _on_debug_skip_button_pressed() -> void:
 		return
 	_auto_skip_active = true
 	var starting_round: int = GameFlow.round_number
-	while GameFlow.round_number == starting_round and not piece_placement_phase.is_debug_finished():
+	while GameFlow.round_number == starting_round and not pion_placement_phase.is_debug_finished():
 		if narration_box.has_options():
 			narration_box.skip()
 		else:
-			piece_placement_phase.force_skip()
+			pion_placement_phase.force_skip()
 		await get_tree().process_frame
 	_auto_skip_active = false
 
@@ -293,8 +293,8 @@ func _on_debug_draw_cards_button_pressed() -> void:
 	if not _debug_card_preview_active:
 		_debug_card_preview_active = true
 		debug_draw_cards_button.text = "Reprendre"
-		_debug_selection_panel_was_visible = piece_selection_panel.visible
-		piece_selection_panel.visible = false
+		_debug_selection_panel_was_visible = pion_selection_panel.visible
+		pion_selection_panel.visible = false
 		debug_preview_camera.make_current()
 		get_tree().paused = true
 		card_draw_phase.start(self, false)
@@ -303,7 +303,7 @@ func _on_debug_draw_cards_button_pressed() -> void:
 		debug_draw_cards_button.text = "Piocher"
 		get_tree().paused = false
 		camera.make_current()
-		piece_selection_panel.visible = _debug_selection_panel_was_visible
+		pion_selection_panel.visible = _debug_selection_panel_was_visible
 
 
 func _on_setup_player_confirmed(player_name: String, color: String) -> void:
@@ -411,16 +411,16 @@ func token_count_for_player_count(player_count: int) -> int:
 
 
 ## Appelé quand tous les joueurs ont posé et résolu leurs deux pièces
-## (piece_placement_phase.finished) : enchaîne directement sur le tour
+## (pion_placement_phase.finished) : enchaîne directement sur le tour
 ## suivant (nouvelles cartes de mer révélées, puis pose de pièces), ou
 ## termine la partie s'il ne reste plus aucun jeton sur les mers.
 func _on_round_finished() -> void:
-	_autosave("pieces")
+	_autosave("pions")
 	if _all_sea_tokens_taken():
 		_end_game()
 		return
 	_start_round()
-	await piece_selection_panel.play_turn_announcement(GameFlow.round_number)
+	await pion_selection_panel.play_turn_announcement(GameFlow.round_number)
 	card_draw_phase.start(self)
 
 
@@ -641,7 +641,7 @@ func _serialize_state(phase: String) -> Dictionary:
 		sea_order.append(SEA_KEY_BY_NODE_NAME.get(tile.name, ""))
 	var action_spots_data: Array = []
 	for spot in action_spots_container.get_children():
-		action_spots_data.append(spot.get_pieces_snapshot())
+		action_spots_data.append(spot.get_pions_snapshot())
 	var hideouts_data: Array = []
 	for spot in hideout_spots_container.get_children():
 		hideouts_data.append(spot.owner_color if spot.is_taken else "")
@@ -760,12 +760,12 @@ func _restore_from_save() -> void:
 	for i in range(action_spots.size()):
 		if i >= action_data.size():
 			continue
-		for piece_info in action_data[i]:
-			var scene: PackedScene = CAPTAIN_PIECE_SCENE if piece_info["rank"] == GameFlow.PieceRank.CAPTAIN else SECOND_PIECE_SCENE
-			var piece: Node2D = scene.instantiate()
-			piece.modulate = GameFlow.COLOR_VALUES[piece_info["color"]]
-			piece.scale = Vector2.ONE * UiTheme.PIECE_SCALE
-			action_spots[i].add_piece(piece, piece_info["color"], piece_info["rank"], false)
+		for pion_info in action_data[i]:
+			var scene: PackedScene = CAPTAIN_PION_SCENE if pion_info["rank"] == GameFlow.PionRank.CAPTAIN else OFFICER_PION_SCENE
+			var pion: Node2D = scene.instantiate()
+			pion.modulate = GameFlow.COLOR_VALUES[pion_info["color"]]
+			pion.scale = Vector2.ONE * UiTheme.PION_SCALE
+			action_spots[i].add_pion(pion, pion_info["color"], pion_info["rank"], false)
 
 	_refresh_player_boards()
 	for p in GameFlow.players:
@@ -777,11 +777,11 @@ func _restore_from_save() -> void:
 		card_draw_phase.start(self)
 	)
 	card_draw_phase.finished.connect(func():
-		_autosave("pieces")
-		piece_placement_phase.start(self)
+		_autosave("pions")
+		pion_placement_phase.start(self)
 	)
 
 	match data.get("phase", "cards"):
 		"hideout": hideout_phase.resume(self)
-		"pieces": piece_placement_phase.resume(self)
+		"pions": pion_placement_phase.resume(self)
 		_: card_draw_phase.start(self)
diff --git a/scripts/board/narration_box.gd b/scripts/board/narration_box.gd
index c30c622..8ac67a4 100644
--- a/scripts/board/narration_box.gd
+++ b/scripts/board/narration_box.gd
@@ -14,7 +14,7 @@ const LABEL_WIDTH := 220.0
 const BUTTON_HEIGHT := 44.0
 const BUTTON_FONT_SIZE := 16
 ## Filtre noir semi-transparent (au lieu d'un fond blanc opaque), même
-## principe que le fondu noir de l'annonce "Tour X" (piece_selection_panel).
+## principe que le fondu noir de l'annonce "Tour X" (pion_selection_panel).
 const PANEL_FILTER_ALPHA := 0.75
 ## Épaisseur du contour indiquant la couleur du joueur dont c'est le tour.
 const OUTLINE_WIDTH := 4
diff --git a/scripts/board/pieces/captain_piece.gd b/scripts/board/pieces/captain_piece.gd
deleted file mode 100644
index bbc573e..0000000
--- a/scripts/board/pieces/captain_piece.gd
+++ /dev/null
@@ -1,10 +0,0 @@
-extends Sprite2D
-
-## Effet 3D (épaisseur) : scripts/common/piece_thickness.gd (constantes
-## partagées avec second_piece.gd et piece_selection_panel.gd).
-func _ready() -> void:
-	if texture:
-		centered = true
-		offset = Vector2(0, -texture.get_height() / 2.0)
-
-	PieceThickness.add_to_sprite(self)
diff --git a/scripts/board/pieces/second_piece.gd b/scripts/board/pieces/second_piece.gd
deleted file mode 100644
index 69829f4..0000000
--- a/scripts/board/pieces/second_piece.gd
+++ /dev/null
@@ -1,10 +0,0 @@
-extends Sprite2D
-
-## Effet 3D (épaisseur) : scripts/common/piece_thickness.gd (constantes
-## partagées avec captain_piece.gd et piece_selection_panel.gd).
-func _ready() -> void:
-	if texture:
-		centered = true
-		offset = Vector2(0, -texture.get_height() / 2.0)
-
-	PieceThickness.add_to_sprite(self)
diff --git a/scripts/board/piece_placement_phase.gd b/scripts/board/pion_placement_phase.gd
similarity index 63%
rename from scripts/board/piece_placement_phase.gd
rename to scripts/board/pion_placement_phase.gd
index ad6093c..5ab7f53 100644
--- a/scripts/board/piece_placement_phase.gd
+++ b/scripts/board/pion_placement_phase.gd
@@ -2,8 +2,8 @@ extends Node
 
 signal finished
 
-const CAPTAIN_SCENE := preload("res://scenes/board/pieces/captain_piece.tscn")
-const SECOND_SCENE := preload("res://scenes/board/pieces/second_piece.tscn")
+const CAPTAIN_SCENE := preload("res://scenes/board/pions/captain_pion.tscn")
+const OFFICER_SCENE := preload("res://scenes/board/pions/officer_pion.tscn")
 const DEBUG_TOTAL_ROUNDS := 7
 
 var _board: Board
@@ -25,35 +25,35 @@ func start(board: Board) -> void:
 	_resolving_action = false
 
 	for spot in _board.action_spots_container.get_children():
-		spot.clear_pieces()
+		spot.clear_pions()
 		if not spot.spot_clicked.is_connected(_on_action_spot_clicked):
 			spot.spot_clicked.connect(_on_action_spot_clicked)
 		spot.set_hover_enabled(true)
-	if not _board.piece_selection_panel.piece_selected.is_connected(_on_piece_selected):
-		_board.piece_selection_panel.piece_selected.connect(_on_piece_selected)
-	if not _board.piece_selection_panel.piece_drag_ended.is_connected(_on_piece_drag_ended):
-		_board.piece_selection_panel.piece_drag_ended.connect(_on_piece_drag_ended)
-	if not _board.piece_selection_panel.piece_drag_started.is_connected(_on_piece_drag_started):
-		_board.piece_selection_panel.piece_drag_started.connect(_on_piece_drag_started)
-	if not _board.piece_selection_panel.piece_drag_stopped.is_connected(_on_piece_drag_stopped):
-		_board.piece_selection_panel.piece_drag_stopped.connect(_on_piece_drag_stopped)
+	if not _board.pion_selection_panel.pion_selected.is_connected(_on_pion_selected):
+		_board.pion_selection_panel.pion_selected.connect(_on_pion_selected)
+	if not _board.pion_selection_panel.pion_drag_ended.is_connected(_on_pion_drag_ended):
+		_board.pion_selection_panel.pion_drag_ended.connect(_on_pion_drag_ended)
+	if not _board.pion_selection_panel.pion_drag_started.is_connected(_on_pion_drag_started):
+		_board.pion_selection_panel.pion_drag_started.connect(_on_pion_drag_started)
+	if not _board.pion_selection_panel.pion_drag_stopped.is_connected(_on_pion_drag_stopped):
+		_board.pion_selection_panel.pion_drag_stopped.connect(_on_pion_drag_stopped)
 
-	_begin_player_piece_turn()
+	_begin_player_pion_turn()
 
 
 ## Affiche le panneau de sélection de pièce + recadre la caméra sur la zone
 ## de sélection uniquement quand un tour va effectivement suivre (si la
 ## phase se termine à la place, on reste sur la vue par défaut : cf le
 ## "return" avant ces lignes).
-func _begin_player_piece_turn() -> void:
+func _begin_player_pion_turn() -> void:
 	if _current_player_index >= GameFlow.players.size():
 		_current_player_index = 0
 		_current_round += 1
 		if _current_round > 1:
-			_end_piece_placement_phase()
+			_end_pion_placement_phase()
 			return
 
-	_board.piece_selection_panel.show_for_placement_phase()
+	_board.pion_selection_panel.show_for_placement_phase()
 	_shift_camera_for_selection(true)
 
 	var player: Dictionary = GameFlow.players[_current_player_index]
@@ -61,16 +61,16 @@ func _begin_player_piece_turn() -> void:
 	_selected_rank = -1
 
 	if _current_round == 0:
-		_board.narration_box.say_with_player(tr("Tour de %s : choisis la pièce à jouer (capitaine ou second)."), player)
-		_board.piece_selection_panel.setup_for_player(color, -1)
+		_board.narration_box.say_with_player(tr("Tour de %s : choisis le pion à jouer (capitaine ou officier)."), player)
+		_board.pion_selection_panel.setup_for_player(color, -1)
 	else:
 		var placed_rank: int = _placed_rank_by_player[_current_player_index]
-		var forced_rank: int = GameFlow.PieceRank.SECOND if placed_rank == GameFlow.PieceRank.CAPTAIN else GameFlow.PieceRank.CAPTAIN
+		var forced_rank: int = GameFlow.PionRank.OFFICER if placed_rank == GameFlow.PionRank.CAPTAIN else GameFlow.PionRank.CAPTAIN
 		_board.narration_box.say_with_player(tr("Tour de %s : place ta dernière pièce."), player)
-		_board.piece_selection_panel.setup_for_player(color, forced_rank)
+		_board.pion_selection_panel.setup_for_player(color, forced_rank)
 
 
-func _on_piece_selected(rank: int) -> void:
+func _on_pion_selected(rank: int) -> void:
 	_selected_rank = rank
 
 
@@ -78,16 +78,16 @@ func _on_piece_selected(rank: int) -> void:
 ## qu'il commence à draguer une pièce, pour bien montrer où elle va
 ## atterrir au survol (en plus du zoom habituel, cf action_spot.gd
 ## set_drag_hover_color).
-func _on_piece_drag_started(_rank: int) -> void:
+func _on_pion_drag_started(_rank: int) -> void:
 	var player: Dictionary = GameFlow.players[_current_player_index]
 	var color: Color = GameFlow.COLOR_VALUES[player["color"]]
 	for spot in _board.action_spots_container.get_children():
 		spot.set_drag_hover_color(color)
 
 
-## Retire la teinte de survol posée par _on_piece_drag_started, que le drag
+## Retire la teinte de survol posée par _on_pion_drag_started, que le drag
 ## se soit terminé par une pose de pièce ou par un simple relâchement.
-func _on_piece_drag_stopped() -> void:
+func _on_pion_drag_stopped() -> void:
 	for spot in _board.action_spots_container.get_children():
 		spot.set_drag_hover_color(null)
 
@@ -101,23 +101,23 @@ func force_skip() -> void:
 		return
 	if _selected_rank == -1:
 		if _current_round == 0:
-			_selected_rank = GameFlow.PieceRank.CAPTAIN
+			_selected_rank = GameFlow.PionRank.CAPTAIN
 		else:
 			var placed_rank: int = _placed_rank_by_player[_current_player_index]
-			_selected_rank = GameFlow.PieceRank.SECOND if placed_rank == GameFlow.PieceRank.CAPTAIN else GameFlow.PieceRank.CAPTAIN
+			_selected_rank = GameFlow.PionRank.OFFICER if placed_rank == GameFlow.PionRank.CAPTAIN else GameFlow.PionRank.CAPTAIN
 
 	var player: Dictionary = GameFlow.players[_current_player_index]
 	for spot in _board.action_spots_container.get_children():
-		if not spot.has_player_piece(player["color"]):
+		if not spot.has_player_pion(player["color"]):
 			_on_action_spot_clicked(spot)
 			return
 
 
 ## Fin d'un drag démarré dans le panneau et relâché hors de celui-ci
-## (piece_selection_panel.gd). On ne pose la pièce que si la souris (pas le
+## (pion_selection_panel.gd). On ne pose la pièce que si la souris (pas le
 ## point-fantôme) touche la collision d'une case au moment du relâchement,
 ## repéré via action_spot.is_hovering() (même état que l'effet de zoom).
-func _on_piece_drag_ended(rank: int) -> void:
+func _on_pion_drag_ended(rank: int) -> void:
 	var spot := _find_hovered_spot()
 	if spot == null:
 		return
@@ -138,27 +138,27 @@ func _on_action_spot_clicked(spot: Node2D) -> void:
 
 	var player: Dictionary = GameFlow.players[_current_player_index]
 
-	if spot.has_player_piece(player["color"]):
-		_board.narration_box.say(tr("Tu ne peux pas poser tes deux pièces sur la même case."))
+	if spot.has_player_pion(player["color"]):
+		_board.narration_box.say(tr("Tu ne peux pas poser tes deux pions sur la même case."))
 		return
 
-	var piece_scene: PackedScene = CAPTAIN_SCENE if _selected_rank == GameFlow.PieceRank.CAPTAIN else SECOND_SCENE
-	var piece: Node2D = piece_scene.instantiate()
-	piece.modulate = GameFlow.COLOR_VALUES[player["color"]]
-	piece.scale = Vector2.ONE * UiTheme.PIECE_SCALE
-	spot.add_piece(piece, player["color"], _selected_rank)
+	var pion_scene: PackedScene = CAPTAIN_SCENE if _selected_rank == GameFlow.PionRank.CAPTAIN else OFFICER_SCENE
+	var pion: Node2D = pion_scene.instantiate()
+	pion.modulate = GameFlow.COLOR_VALUES[player["color"]]
+	pion.scale = Vector2.ONE * UiTheme.PION_SCALE
+	spot.add_pion(pion, player["color"], _selected_rank)
 	_board.narration_box.hide_box()
 
 	if _current_round == 0:
 		_placed_rank_by_player[_current_player_index] = _selected_rank
 	_selected_rank = -1
-	_board._autosave("pieces")
+	_board._autosave("pions")
 
 	# On quitte la vue "sélection de pièce" (zoom + panneau) dès que la pièce
 	# est posée : la résolution d'action (ex. cliquer sur une mer pour le
 	# déplacement) a besoin de la vue par défaut sur tout le plateau. On y
-	# revient dans _begin_player_piece_turn() pour le joueur suivant.
-	_board.piece_selection_panel.hide_panel()
+	# revient dans _begin_player_pion_turn() pour le joueur suivant.
+	_board.pion_selection_panel.hide_panel()
 	_shift_camera_for_selection(false)
 
 	var spot_index: int = spot.get_index()
@@ -167,7 +167,7 @@ func _on_action_spot_clicked(spot: Node2D) -> void:
 	_resolving_action = false
 
 	_current_player_index += 1
-	_begin_player_piece_turn()
+	_begin_player_pion_turn()
 
 
 ## Vrai une fois les DEBUG_TOTAL_ROUNDS manches de test écoulées (utilisé par
@@ -176,13 +176,13 @@ func is_debug_finished() -> bool:
 	return _debug_round_index >= DEBUG_TOTAL_ROUNDS
 
 
-func _end_piece_placement_phase() -> void:
+func _end_pion_placement_phase() -> void:
 	for spot in _board.action_spots_container.get_children():
 		spot.set_hover_enabled(false)
-	_board.piece_selection_panel.hide_panel()
+	_board.pion_selection_panel.hide_panel()
 	_shift_camera_for_selection(false)
 
-	if _board.debug_skip_to_pieces:
+	if _board.debug_skip_to_pions:
 		_round_transitioning = true
 		_debug_round_index += 1
 		if _debug_round_index < DEBUG_TOTAL_ROUNDS:
@@ -211,34 +211,34 @@ func resume(board: Board) -> void:
 	_board = board
 	_board.debug_skip_button.visible = GameFlow.is_debug_mode
 	_placed_rank_by_player.clear()
-	var total_pieces := 0
+	var total_pions := 0
 	for i in range(GameFlow.players.size()):
 		var color: String = GameFlow.players[i]["color"]
 		var count := 0
 		var known_rank := -1
 		for spot in _board.action_spots_container.get_children():
-			for p in spot.get_pieces_snapshot():
+			for p in spot.get_pions_snapshot():
 				if p["color"] == color:
 					count += 1
 					known_rank = p["rank"]
-		total_pieces += count
+		total_pions += count
 		if count == 1:
 			_placed_rank_by_player[i] = known_rank
 
 	var n: int = GameFlow.players.size()
-	_current_round = 0 if total_pieces < n else 1
-	_current_player_index = total_pieces if _current_round == 0 else total_pieces - n
+	_current_round = 0 if total_pions < n else 1
+	_current_player_index = total_pions if _current_round == 0 else total_pions - n
 
 	for spot in _board.action_spots_container.get_children():
 		if not spot.spot_clicked.is_connected(_on_action_spot_clicked):
 			spot.spot_clicked.connect(_on_action_spot_clicked)
 		spot.set_hover_enabled(true)
-	if not _board.piece_selection_panel.piece_selected.is_connected(_on_piece_selected):
-		_board.piece_selection_panel.piece_selected.connect(_on_piece_selected)
-	if not _board.piece_selection_panel.piece_drag_ended.is_connected(_on_piece_drag_ended):
-		_board.piece_selection_panel.piece_drag_ended.connect(_on_piece_drag_ended)
-	if not _board.piece_selection_panel.piece_drag_started.is_connected(_on_piece_drag_started):
-		_board.piece_selection_panel.piece_drag_started.connect(_on_piece_drag_started)
-	if not _board.piece_selection_panel.piece_drag_stopped.is_connected(_on_piece_drag_stopped):
-		_board.piece_selection_panel.piece_drag_stopped.connect(_on_piece_drag_stopped)
-	_begin_player_piece_turn()
+	if not _board.pion_selection_panel.pion_selected.is_connected(_on_pion_selected):
+		_board.pion_selection_panel.pion_selected.connect(_on_pion_selected)
+	if not _board.pion_selection_panel.pion_drag_ended.is_connected(_on_pion_drag_ended):
+		_board.pion_selection_panel.pion_drag_ended.connect(_on_pion_drag_ended)
+	if not _board.pion_selection_panel.pion_drag_started.is_connected(_on_pion_drag_started):
+		_board.pion_selection_panel.pion_drag_started.connect(_on_pion_drag_started)
+	if not _board.pion_selection_panel.pion_drag_stopped.is_connected(_on_pion_drag_stopped):
+		_board.pion_selection_panel.pion_drag_stopped.connect(_on_pion_drag_stopped)
+	_begin_player_pion_turn()
diff --git a/scripts/board/pions/captain_pion.gd b/scripts/board/pions/captain_pion.gd
new file mode 100644
index 0000000..221a8b0
--- /dev/null
+++ b/scripts/board/pions/captain_pion.gd
@@ -0,0 +1,10 @@
+extends Sprite2D
+
+## Effet 3D (épaisseur) : scripts/common/pion_thickness.gd (constantes
+## partagées avec officer_pion.gd et pion_selection_panel.gd).
+func _ready() -> void:
+	if texture:
+		centered = true
+		offset = Vector2(0, -texture.get_height() / 2.0)
+
+	PionThickness.add_to_sprite(self)
diff --git a/scripts/board/pieces/captain_piece.gd.uid b/scripts/board/pions/captain_pion.gd.uid
similarity index 100%
rename from scripts/board/pieces/captain_piece.gd.uid
rename to scripts/board/pions/captain_pion.gd.uid
diff --git a/scripts/board/pions/officer_pion.gd b/scripts/board/pions/officer_pion.gd
new file mode 100644
index 0000000..b2ddbf5
--- /dev/null
+++ b/scripts/board/pions/officer_pion.gd
@@ -0,0 +1,10 @@
+extends Sprite2D
+
+## Effet 3D (épaisseur) : scripts/common/pion_thickness.gd (constantes
+## partagées avec captain_pion.gd et pion_selection_panel.gd).
+func _ready() -> void:
+	if texture:
+		centered = true
+		offset = Vector2(0, -texture.get_height() / 2.0)
+
+	PionThickness.add_to_sprite(self)
diff --git a/scripts/board/pieces/second_piece.gd.uid b/scripts/board/pions/officer_pion.gd.uid
similarity index 100%
rename from scripts/board/pieces/second_piece.gd.uid
rename to scripts/board/pions/officer_pion.gd.uid
diff --git a/scripts/common/piece_thickness.gd b/scripts/common/pion_thickness.gd
similarity index 94%
rename from scripts/common/piece_thickness.gd
rename to scripts/common/pion_thickness.gd
index bfec2d3..21cd7e2 100644
--- a/scripts/common/piece_thickness.gd
+++ b/scripts/common/pion_thickness.gd
@@ -1,7 +1,7 @@
-class_name PieceThickness
+class_name PionThickness
 
-## Constantes PARTAGÉES par le plateau (captain_piece.gd/second_piece.gd) et
-## le panneau de sélection (piece_selection_panel.gd) : un seul endroit à
+## Constantes PARTAGÉES par le plateau (captain_pion.gd/officer_pion.gd) et
+## le panneau de sélection (pion_selection_panel.gd) : un seul endroit à
 ## modifier pour ajuster l'effet partout. thickness_px est TOUJOURS exprimé
 ## en pixels ÉCRAN (affichés), quelle que soit l'échelle du node porteur.
 const THICKNESS_PX := 10.0
diff --git a/scripts/resources/data/activity_boards/activity_board_bleue_brune.tres b/scripts/resources/data/activity_boards/activity_board_bleue_brune.tres
index 102ab4f..e232fb7 100644
--- a/scripts/resources/data/activity_boards/activity_board_bleue_brune.tres
+++ b/scripts/resources/data/activity_boards/activity_board_bleue_brune.tres
@@ -5,6 +5,6 @@
 
 [resource]
 script = ExtResource("1_yuxt4")
-id = "bleu_claire"
+id = "bleue_brune"
 texture = ExtResource("2_lmmuu")
 metadata/_custom_type_script = "uid://c35kddoarri0x"
diff --git a/scripts/resources/data/activity_boards/activity_board_brune.tres b/scripts/resources/data/activity_boards/activity_board_brune.tres
index 79b9e11..460f9f7 100644
--- a/scripts/resources/data/activity_boards/activity_board_brune.tres
+++ b/scripts/resources/data/activity_boards/activity_board_brune.tres
@@ -5,6 +5,6 @@
 
 [resource]
 script = ExtResource("1_4sb2b")
-id = "claire"
+id = "brune"
 texture = ExtResource("2_g2sak")
 metadata/_custom_type_script = "uid://c35kddoarri0x"
diff --git a/scripts/resources/data/card_catalog.gd b/scripts/resources/data/card_catalog.gd
index fc44ad9..7771515 100644
--- a/scripts/resources/data/card_catalog.gd
+++ b/scripts/resources/data/card_catalog.gd
@@ -21,8 +21,8 @@ extends RefCounted
 
 const ACTIVITY_BOARDS := {
 	"rouge": "res://scripts/resources/data/activity_boards/activity_board_rouge.tres",
-	"claire": "res://scripts/resources/data/activity_boards/activity_board_claire.tres",
-	"bleu_claire": "res://scripts/resources/data/activity_boards/activity_board_bleu_claire.tres",
+	"brune": "res://scripts/resources/data/activity_boards/activity_board_brune.tres",
+	"bleue_brune": "res://scripts/resources/data/activity_boards/activity_board_bleue_brune.tres",
 }
 
 const DEFINITIONS := [
@@ -31,11 +31,11 @@ const DEFINITIONS := [
 		"title": "Navire fantôme", "description": "Un navire spectral vous encercle. Lancez 3 dés pour tenter de fuir.",
 	},
 	{
-		"sea": "sauvage", "type": GameCard.CardType.ILE, "planche": "claire",
+		"sea": "sauvage", "type": GameCard.CardType.ILE, "planche": "brune",
 		"title": "Île abandonnée", "description": "Une île silencieuse apparaît à l'horizon.",
 	},
 	{
-		"sea": "sauvage", "type": GameCard.CardType.PORT, "planche": "bleu_claire",
+		"sea": "sauvage", "type": GameCard.CardType.PORT, "planche": "bleue_brune",
 		"title": "Petit port de pêcheurs", "description": "Vous accostez dans un port paisible.",
 	},
 ]
diff --git a/scripts/ui/piece_selection_panel.gd b/scripts/ui/pion_selection_panel.gd
similarity index 89%
rename from scripts/ui/piece_selection_panel.gd
rename to scripts/ui/pion_selection_panel.gd
index d5ce0e4..69fa8ee 100644
--- a/scripts/ui/piece_selection_panel.gd
+++ b/scripts/ui/pion_selection_panel.gd
@@ -1,19 +1,19 @@
 extends Control
 
-signal piece_selected(rank: int)
+signal pion_selected(rank: int)
 ## Émis dès qu'un drag (potentiel) démarre sur une pièce, avant même de
-## savoir s'il quittera le panneau ou non (board.gd/piece_placement_phase.gd
+## savoir s'il quittera le panneau ou non (board.gd/pion_placement_phase.gd
 ## s'en sert pour teinter les cases d'action avec la couleur du joueur
 ## pendant qu'il drague).
-signal piece_drag_started(rank: int)
+signal pion_drag_started(rank: int)
 ## Émis quand un drag démarré sur une pièce se termine hors du panneau
-## (relâchement sur le plateau). piece_placement_phase.gd écoute ce signal
+## (relâchement sur le plateau). pion_placement_phase.gd écoute ce signal
 ## pour savoir si la souris était alors au-dessus d'une case valide.
-signal piece_drag_ended(rank: int)
+signal pion_drag_ended(rank: int)
 ## Émis à la toute fin de CHAQUE drag (relâché dans le panneau ou dehors),
-## contrairement à piece_drag_ended qui ne l'est que dans le 2e cas :
+## contrairement à pion_drag_ended qui ne l'est que dans le 2e cas :
 ## utilisé pour toujours nettoyer la teinte de survol posée sur les cases.
-signal piece_drag_stopped
+signal pion_drag_stopped
 
 const HOVER_SCALE := 1.1
 const SELECTED_SCALE := 1.2
@@ -21,8 +21,8 @@ const TWEEN_DURATION := 0.15
 const UNSELECTED_TINT := Color(0.55, 0.55, 0.55)
 
 ## Effet 3D (épaisseur) des icônes capitaine/second : scripts/common/
-## piece_thickness.gd (constantes partagées avec captain_piece.gd/
-## second_piece.gd). Nodes enfants de btn -> suivent automatiquement son
+## pion_thickness.gd (constantes partagées avec captain_pion.gd/
+## officer_pion.gd). Nodes enfants de btn -> suivent automatiquement son
 ## scale (survol/sélection, _tween_scale) et son modulate (_refresh_colors).
 
 ## Annonce de tour : filtre noir + texte/jeton en perspective.
@@ -51,7 +51,7 @@ const DRAG_ROTATE_SPEED_DEG := 140.0
 var background: ColorRect
 var icons_box: VBoxContainer
 var captain_button: TextureButton
-var second_button: TextureButton
+var officer_button: TextureButton
 var splatter: TextureRect
 
 ## Overlay plein écran indépendant du rect du panneau (posé dans son propre
@@ -97,14 +97,14 @@ func _ready() -> void:
 	icons_box.add_theme_constant_override("separation", 60)
 	add_child(icons_box)
 
-	captain_button = _build_piece_option("Capitaine", preload("res://assets/art/pieces/capitaine.png"))
-	second_button = _build_piece_option("Second", preload("res://assets/art/pieces/second.png"))
+	captain_button = _build_pion_option("Capitaine", preload("res://assets/art/pieces/capitaine.png"))
+	officer_button = _build_pion_option("Officier", preload("res://assets/art/pieces/second.png"))
 
-	captain_button.pressed.connect(func(): piece_selected.emit(GameFlow.PieceRank.CAPTAIN))
-	second_button.pressed.connect(func(): piece_selected.emit(GameFlow.PieceRank.SECOND))
+	captain_button.pressed.connect(func(): pion_selected.emit(GameFlow.PionRank.CAPTAIN))
+	officer_button.pressed.connect(func(): pion_selected.emit(GameFlow.PionRank.OFFICER))
 
-	captain_button.button_down.connect(_on_piece_button_down.bind(GameFlow.PieceRank.CAPTAIN, captain_button))
-	second_button.button_down.connect(_on_piece_button_down.bind(GameFlow.PieceRank.SECOND, second_button))
+	captain_button.button_down.connect(_on_pion_button_down.bind(GameFlow.PionRank.CAPTAIN, captain_button))
+	officer_button.button_down.connect(_on_pion_button_down.bind(GameFlow.PionRank.OFFICER, officer_button))
 
 	get_viewport().size_changed.connect(_layout)
 	_layout()
@@ -118,7 +118,7 @@ func _process(delta: float) -> void:
 		_update_drag(delta)
 
 
-func _build_piece_option(label_text: String, texture: Texture2D) -> TextureButton:
+func _build_pion_option(label_text: String, texture: Texture2D) -> TextureButton:
 	var option := VBoxContainer.new()
 	option.alignment = BoxContainer.ALIGNMENT_CENTER
 	option.add_theme_constant_override("separation", 8)
@@ -138,7 +138,7 @@ func _build_piece_option(label_text: String, texture: Texture2D) -> TextureButto
 	btn.custom_minimum_size = Vector2(UiTheme.SELECTION_ICON_HEIGHT * aspect, UiTheme.SELECTION_ICON_HEIGHT)
 	btn.pivot_offset = btn.custom_minimum_size / 2.0
 
-	PieceThickness.add_to_control(btn, texture, btn.custom_minimum_size)
+	PionThickness.add_to_control(btn, texture, btn.custom_minimum_size)
 
 	btn.mouse_entered.connect(_on_button_hover.bind(btn, true))
 	btn.mouse_exited.connect(_on_button_hover.bind(btn, false))
@@ -183,27 +183,27 @@ func hide_panel() -> void:
 ## drag ne peut pas démarrer non plus.
 func set_interactive(enabled: bool) -> void:
 	captain_button.disabled = not enabled
-	second_button.disabled = not enabled
+	officer_button.disabled = not enabled
 
 
 ## only_rank == -1 -> les deux pièces proposées, capitaine sélectionné par défaut.
 ## only_rank == CAPTAIN/SECOND -> une seule affichée, forcée/sélectionnée.
 func setup_for_player(color: Color, only_rank: int = -1) -> void:
 	_current_color = color
-	captain_button.get_parent().visible = only_rank == -1 or only_rank == GameFlow.PieceRank.CAPTAIN
-	second_button.get_parent().visible = only_rank == -1 or only_rank == GameFlow.PieceRank.SECOND
+	captain_button.get_parent().visible = only_rank == -1 or only_rank == GameFlow.PionRank.CAPTAIN
+	officer_button.get_parent().visible = only_rank == -1 or only_rank == GameFlow.PionRank.OFFICER
 
-	for btn in [captain_button, second_button]:
+	for btn in [captain_button, officer_button]:
 		btn.button_pressed = false
 		btn.scale = Vector2.ONE
 
 	if only_rank != -1:
-		var forced_btn: TextureButton = captain_button if only_rank == GameFlow.PieceRank.CAPTAIN else second_button
+		var forced_btn: TextureButton = captain_button if only_rank == GameFlow.PionRank.CAPTAIN else officer_button
 		forced_btn.button_pressed = true
-		piece_selected.emit(only_rank)
+		pion_selected.emit(only_rank)
 	else:
 		captain_button.button_pressed = true
-		piece_selected.emit(GameFlow.PieceRank.CAPTAIN)
+		pion_selected.emit(GameFlow.PionRank.CAPTAIN)
 
 	_refresh_colors()
 
@@ -221,7 +221,7 @@ func _on_button_toggled(is_pressed: bool, btn: TextureButton) -> void:
 ## Sélectionnée = couleur pleine. Non sélectionnée = couleur grisée.
 ## Plus de filtre noir supplémentaire au survol (le gris suffit déjà).
 func _refresh_colors() -> void:
-	for btn in [captain_button, second_button]:
+	for btn in [captain_button, officer_button]:
 		btn.modulate = _current_color if btn.button_pressed else _current_color * UNSELECTED_TINT
 
 
@@ -369,12 +369,12 @@ func _build_drag_ghost() -> void:
 	layer.add_child(_drag_ghost)
 
 
-func _on_piece_button_down(rank: int, btn: TextureButton) -> void:
+func _on_pion_button_down(rank: int, btn: TextureButton) -> void:
 	_dragging = true
 	_drag_rank = rank
 	_drag_button = btn
 	_drag_rotation_deg = 0.0
-	piece_drag_started.emit(rank)
+	pion_drag_started.emit(rank)
 
 
 ## Appelé chaque frame tant qu'un drag est en cours. Tant que la souris
@@ -402,7 +402,7 @@ func _update_drag(delta: float) -> void:
 
 
 ## Relâchement du drag. Si la souris était sur le plateau (hors panneau),
-## on émet piece_drag_ended : piece_placement_phase.gd vérifie alors si une
+## on émet pion_drag_ended : pion_placement_phase.gd vérifie alors si une
 ## case (action_spot) est actuellement survolée pour poser la pièce. Si la
 ## souris était encore dans le panneau, on annule simplement (rien à faire,
 ## l'icône est déjà réapparue dans _update_drag).
@@ -415,8 +415,8 @@ func _end_drag(over_panel: bool) -> void:
 	_drag_button = null
 	_drag_rank = -1
 	if not over_panel:
-		piece_drag_ended.emit(rank)
-	piece_drag_stopped.emit()
+		pion_drag_ended.emit(rank)
+	pion_drag_stopped.emit()
 
 
 func _on_drag_ghost_draw() -> void:
diff --git a/scripts/ui/player_board_expanded.gd b/scripts/ui/player_board_expanded.gd
index 7983470..9d33faa 100644
--- a/scripts/ui/player_board_expanded.gd
+++ b/scripts/ui/player_board_expanded.gd
@@ -51,7 +51,7 @@ const SPECIAL_TOKEN_MAX_ATTEMPTS := 40
 ## cubes de ressource DOIVENT utiliser la direction opposée à celle-ci.
 ## Épaisseur totale des jetons Fortune/Trésor, en pixels écran (~3px demandés).
 ## Géré par empilement de TextureRect (_add_token_with_thickness ci-dessous),
-## comme les pièces capitaine/second (scripts/common/piece_thickness.gd) et
+## comme les pièces capitaine/second (scripts/common/pion_thickness.gd) et
 ## les bateaux (hideout_spot.gd). Ici les nodes restent séparés (pas enfants
 ## les uns des autres) car ils doivent être déplacés ensemble pendant un
 ## glisser-déposer via le tableau "nodes" de _draggable_items.
