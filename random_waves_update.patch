diff --git a/shaders/ocean_waves_background.gdshader b/shaders/ocean_waves_background.gdshader
index 92e6dd2..def2d51 100644
--- a/shaders/ocean_waves_background.gdshader
+++ b/shaders/ocean_waves_background.gdshader
@@ -1,19 +1,30 @@
 shader_type canvas_item;
 
 // Fond animé pour l'écran de jeu (board.tscn) : de nombreuses sinusoïdes
-// remplies (zone sous la courbe colorée, pas juste un trait) empilées du
-// haut vers le bas de l'écran et mélangées entre elles par transparence,
-// pour un effet de vagues superposées. Chaque courbe a sa propre
-// fréquence, sa propre vitesse (alternée gauche/droite) et sa propre
-// teinte.
+// remplies, superposées par transparence. Toutes les valeurs (fréquence,
+// vitesse, sens, position, amplitude, teinte) sont tirées aléatoirement
+// par couche (hash de l'index) : pas de progression liée à l'ordre des
+// couches, seulement une amplitude commune avec un peu de bruit ajouté.
 
 uniform vec4 background_color : source_color = vec4(0.01, 0.03, 0.10, 1.0);
 uniform float speed : hint_range(0.0, 2.0) = 0.35;
-uniform float amplitude : hint_range(0.0, 0.3) = 0.06;
+// Amplitude commune : toutes les vagues partent de cette taille (celle
+// des plus grosses), avec +/- amplitude_jitter de bruit aléatoire.
+uniform float amplitude : hint_range(0.0, 0.3) = 0.1;
+uniform float amplitude_jitter : hint_range(0.0, 0.2) = 0.05;
 uniform float base_hue : hint_range(0.0, 1.0) = 0.55;
-uniform float layer_alpha : hint_range(0.0, 1.0) = 0.35;
+uniform float hue_spread : hint_range(0.0, 1.0) = 0.4;
+uniform float layer_alpha : hint_range(0.0, 1.0) = 0.2;
+uniform float overall_alpha : hint_range(0.0, 1.0) = 0.85;
 
-const int WAVE_COUNT = 14;
+const int WAVE_COUNT = 16;
+
+float hash11(float p) {
+	p = fract(p * 0.1031);
+	p *= p + 33.33;
+	p *= p + p;
+	return fract(p);
+}
 
 vec3 hsv2rgb(vec3 c) {
 	vec4 k = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);
@@ -27,37 +38,45 @@ void fragment() {
 
 	for (int i = 0; i < WAVE_COUNT; i++) {
 		float fi = float(i);
-		// Progression 0 (haut de l'écran) -> 1 (bas de l'écran).
-		float fn = fi / float(WAVE_COUNT - 1);
-
-		// Sens de défilement alterné : lignes paires vers la droite,
-		// impaires vers la gauche.
-		float dir = mod(fi, 2.0) < 0.5 ? 1.0 : -1.0;
-		// Fréquence et vitesse de phase différentes par courbe.
-		float freq = 2.5 + fi * 1.15;
-		float phase_speed = (0.6 + fi * 0.17) * dir;
-		// Courbes réparties du haut vers le bas, avec un chevauchement
-		// volontaire (amplitude) pour que les couches se superposent.
-		float base_y = 0.05 + fn * 0.9;
-		float amp = amplitude * (0.5 + 0.5 * sin(fi * 1.3 + 1.0));
-
-		float wave_y = base_y + sin(UV.x * freq * TAU + t * phase_speed + fi * 2.3) * amp;
-
-		// Remplit toute la zone SOUS la sinusoïde (pas juste un trait).
+
+		// Chaque propriété est un tirage indépendant (aucun lien avec fi
+		// hormis la graine) : rien ne varie progressivement d'une couche
+		// à l'autre.
+		float r_dir = hash11(fi * 12.9);
+		float r_freq = hash11(fi * 3.7 + 1.0);
+		float r_speed = hash11(fi * 7.1 + 2.0);
+		float r_phase = hash11(fi * 5.3 + 3.0);
+		float r_amp = hash11(fi * 9.7 + 4.0);
+		float r_y = hash11(fi * 4.1 + 5.0);
+		float r_hue = hash11(fi * 6.3 + 6.0);
+		float r_sat = hash11(fi * 8.9 + 7.0);
+		float r_val = hash11(fi * 2.3 + 8.0);
+
+		float dir = r_dir < 0.5 ? 1.0 : -1.0;
+		float freq = 2.5 + r_freq * 9.0;
+		float phase_speed = (0.4 + r_speed * 1.4) * dir;
+
+		// Toutes les vagues ont (à peu près) la taille des plus grosses :
+		// amplitude commune + bruit aléatoire ajouté/retiré, pas de
+		// réduction progressive vers le haut de l'écran.
+		float amp = amplitude + (r_amp * 2.0 - 1.0) * amplitude_jitter;
+
+		float base_y = r_y;
+		float wave_y = base_y + sin(UV.x * freq * TAU + t * phase_speed + r_phase * TAU) * amp;
+
 		float fill = smoothstep(wave_y - 0.004, wave_y + 0.004, UV.y);
 
-		// Teinte qui varie légèrement d'une couche à l'autre, et qui se
-		// fonce/sature vers le bas de l'écran.
-		float hue = mod(base_hue + fn * 0.12 - 0.06, 1.0);
-		float sat = 0.55 + 0.25 * sin(fi * 0.9);
-		float val = 0.35 + 0.5 * fn;
+		// Teinte aléatoire indépendante par couche (pas de dégradé), dans
+		// un voisinage réglable autour de base_hue.
+		float hue = fract(base_hue + (r_hue - 0.5) * hue_spread);
+		float sat = 0.4 + r_sat * 0.5;
+		float val = 0.35 + r_val * 0.55;
 		vec3 layer_color = hsv2rgb(vec3(hue, sat, val));
 
-		// Mélange par transparence : les couches successives se
-		// superposent au lieu de simplement s'écraser les unes sur les
-		// autres.
 		col = mix(col, layer_color, fill * layer_alpha);
 	}
 
-	COLOR = vec4(col, 1.0);
+	// Vagues (et fond) un peu transparents : laisse transparaître ce qu'il
+	// y a derrière ce calque.
+	COLOR = vec4(col, overall_alpha);
 }
